scalar gqlDate

type User {
    _id: ID!
    f_name: String
    l_name: String
    email: String!
    pswd: String
    createdAt: gqlDate
    updatedAt: gqlDate
}

type Query {
    me: User
}

type Mutation {
    login(email: String): String
}
